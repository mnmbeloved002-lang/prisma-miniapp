{
  "name": "Prisma Ritual AI (L4 Hard Mode)",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-22-bullseye",

  "features": {},

  "postCreateCommand": "npm install -g pnpm localtunnel && pnpm install && sudo apt-get update && sudo apt-get install -y chromium curl jq nano procps && sudo curl -L https://github.com/google/osv-scanner/releases/latest/download/osv-scanner_linux_amd64 -o /usr/local/bin/osv-scanner && sudo chmod +x /usr/local/bin/osv-scanner && pnpm exec playwright install chromium",

  "customizations": {
    "vscode": {
      "extensions": ["biomejs.biome", "EditorConfig.EditorConfig"],
      "settings": {
        "files.eol": "\n",
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "biomejs.biome",
        "[typescript]": {
          "editor.defaultFormatter": "biomejs.biome"
        },
        "[typescriptreact]": {
          "editor.defaultFormatter": "biomejs.biome"
        },
        "[javascript]": {
          "editor.defaultFormatter": "biomejs.biome"
        },
        "[javascriptreact]": {
          "editor.defaultFormatter": "biomejs.biome"
        },
        "[json]": {
          "editor.defaultFormatter": "biomejs.biome"
        }
      }
    }
  },

  "remoteUser": "node"
}
