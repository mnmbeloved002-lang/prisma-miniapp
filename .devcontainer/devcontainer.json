{
  "name": "Prisma Ritual AI (L4 Hard Mode)",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:1-22-bullseye",

  "features": {},

  "postCreateCommand": "npm install -g pnpm && sudo apt-get update && sudo apt-get install -y chromium && sudo curl -L https://github.com/google/osv-scanner/releases/latest/download/osv-scanner_linux_amd64 -o /usr/local/bin/osv-scanner && sudo chmod +x /usr/local/bin/osv-scanner && pnpm exec playwright install chromium",

  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "EditorConfig.EditorConfig"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "files.eol": "\n"
      }
    }
  },
  "remoteUser": "node"
}
