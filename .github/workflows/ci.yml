name: Lint + Typecheck + Build

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'pnpm'

      # ✅ Добавляем установку pnpm через официальный экшен
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint + Typecheck
        run: |
          pnpm lint
          pnpm typecheck

      - name: Build
        run: pnpm build
