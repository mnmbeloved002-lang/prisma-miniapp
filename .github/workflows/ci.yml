name: Lint + Typecheck + Build

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ✅ Сначала устанавливаем pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.20.0

      # ✅ Потом настраиваем Node.js (с кэшем pnpm)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint + Typecheck
        run: |
          pnpm lint
          pnpm typecheck

      - name: Build
        run: pnpm build
