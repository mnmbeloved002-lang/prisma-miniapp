# Development Setup

## Prerequisites
- Node.js (use version from .nvmrc)
- pnpm

## Installation
1. `nvm use` (or install Node.js LTS)
2. `pnpm install`
3. `pnpm run build`
4. `pnpm run dev`

## Verification
- `pnpm run sanity` - full quality check
- `pnpm run test:all` - complete test suite
